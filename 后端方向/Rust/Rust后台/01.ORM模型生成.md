## 使用 sea-orm 生成模型

### 1. 安装 sea-orm-cli

```bash
cargo install sea-orm-cli
```

### 2. 生成模型

```bash
sea-orm-cli generate entity \
  -u mysql://user:password@localhost:3306/my_database \
  -o src/entities
  # -t users,posts
```

_Tips: -o 参数标识产出位置，使用 -t 生成指定表的实体_
