## 类型放宽

类型放宽在TS中可以总结为下面的四个原则：

1. 可变性原则： let / var / object / array 默认可变 → 放宽
2. 上下文缺失原则： const fn = (x) => x + 1; 没有明确定义，默认推导为any
3. 安全优先原则：宁可丢失精度，也不允许类型不安全 → 放宽
4. constraint 驱动原则：泛型约束可能改变推导路径，而不仅仅是验证结果
